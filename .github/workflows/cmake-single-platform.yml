name: CMake Cross-Platform Build

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

env:
  BUILD_TYPE: Release

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            build-dir: build
          - os: macos-latest
            build-dir: build
          - os: ubuntu-latest
            build-dir: build

    steps:
    - uses: actions/checkout@v4

    - name: Set up environment
      run: |
        if [ "$RUNNER_OS" = "Windows" ]; then
          choco install cmake
        elif [ "$RUNNER_OS" = "macOS" ]; then
          brew install cmake
        elif [ "$RUNNER_OS" = "Linux" ]; then
          sudo apt-get update
          sudo apt-get install cmake
        fi
      shell: bash

    - uses: actions/cache@v2
      with:
        path: |
          ~\AppData\Local\Temp\chocolatey
          /usr/local/Homebrew
          /var/cache/apt/archives
        key: ${{ runner.os }}-deps-${{ hashFiles('**/CMakeLists.txt') }}
        restore-keys: |
          ${{ runner.os }}-deps-

    - name: Configure CMake
      run: cmake -B ${{ matrix.build-dir }} -DCMAKE_BUILD_TYPE=${{ env.BUILD_TYPE }}
      working-directory: ${{github.workspace}}/src

    - name: Build
      run: cmake --build ${{ matrix.build-dir }} --config ${{ env.BUILD_TYPE }}
      working-directory: ${{github.workspace}}/src

    - name: Upload Artifacts
      uses: actions/upload-artifact@v2
      with:
        name: Compiled-Binaries-${{ matrix.os }}
        path: ${{github.workspace}}/src/${{ matrix.build-dir }}/path-to-your-binaries
